import 'dart:convert';

class SafeKeeper {
  // Encrypt the text with your algorithm
  static String encrypt(String text) {
    return text;
  }

  // Decrypt the text with your algorithm
  static String decrypt(String text) {
    return text;
  }
}
